# Implemented FREDs

Closed Feature Requirement Documents live here.

When a feature is finished implementing:

1. Move `docs/features/{NNN}-{slug}.md` into this folder (same filename).
2. Update [OPEN-FRED-INDEX.md](../OPEN-FRED-INDEX.md): remove the open row, add a Recently closed line, set the Updated date.
3. Do not leave a stub in `docs/features/`.

## Closed

| FRED | Notes |
| --- | --- |
| | |
